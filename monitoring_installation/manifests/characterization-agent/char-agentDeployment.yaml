apiVersion: apps/v1
kind: Deployment
metadata:
  name: char-agent
  namespace: monitoring
  labels:
    app: char-agent
spec:
  replicas: 1 # A Deployment requires a 'replicas' field.
  selector:
    matchLabels:
      app: char-agent
  template:
    metadata:
      labels:
        app: char-agent
    spec:
      hostNetwork: true
      containers:
        - name: char-agent
          image: gkorod/char-agent:v1.2
          imagePullPolicy: Always
          securityContext:
            privileged: true
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - containerPort: 5001
              name: char-agent
          volumeMounts:
            - name: env-file
              mountPath: /characterization-agent/.env
              readOnly: true
      volumes:
        - name: env-file
          hostPath:
            path: /opt/char-agent/.env
            type: File
      imagePullSecrets:
        - name: charagent